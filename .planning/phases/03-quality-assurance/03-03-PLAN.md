---
phase: 03-quality-assurance
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - AltTab/AltTab.xcodeproj/project.pbxproj
autonomous: false
requirements:
  - QA-03
  - QA-05
  - QA-07
  - QA-08

must_haves:
  truths:
    - "Xcode static analyzer warnings are enabled in build settings"
    - "Release configuration treats warnings as errors"
    - "Zero Xcode build warnings in Release configuration"
    - "All QA requirements verified end-to-end"
  artifacts:
    - path: "AltTab/AltTab.xcodeproj/project.pbxproj"
      provides: "Static analysis build settings"
      contains: "CLANG_ANALYZER"
  key_links:
    - from: "AltTab/AltTab.xcodeproj/project.pbxproj"
      to: "AltTab/AltTab/*.swift"
      via: "Xcode build settings apply to all source files"
      pattern: "GCC_TREAT_WARNINGS_AS_ERRORS"
---

<objective>
Enable Xcode static analysis, configure warnings-as-errors for Release, and verify all QA requirements are met.

Purpose: Final quality gate ensuring zero warnings and all bugs resolved.
Output: Static analysis configured, zero warnings, QA phase verified complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-quality-assurance/03-CONTEXT.md
@.planning/phases/03-quality-assurance/03-01-SUMMARY.md
@.planning/phases/03-quality-assurance/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable static analysis and warnings-as-errors in Xcode project</name>
  <files>AltTab/AltTab.xcodeproj/project.pbxproj</files>
  <read_first>
    - AltTab/AltTab.xcodeproj/project.pbxproj
  </read_first>
  <action>
Per D-21, D-22, D-23, modify Xcode build settings via `project.pbxproj`:

1. In the Release build configuration section, add/set these build settings:
   - `GCC_TREAT_WARNINGS_AS_ERRORS = YES` (D-22: warnings as errors in Release)
   - `CLANG_ANALYZER_SECURITY_FLOATLOOPCOUNTER = YES`
   - `CLANG_ANALYZER_SECURITY_INSECUREAPI_RAND = YES`
   - `CLANG_ANALYZER_SECURITY_INSECUREAPI_UNCHECKEDRETURN = YES`
   - `CLANG_ANALYZER_DEADCODE_DEADSTORES = YES` (D-23: dead store)
   - `CLANG_ANALYZER_MEMORY_MANAGEMENT = YES` (D-23: memory leaks)
   - `CLANG_ANALYZER_NONNULL = YES` (D-23: null dereference)
   - `RUN_CLANG_STATIC_ANALYZER = YES` (D-21: enable static analyzer)

2. In the Debug build configuration, enable the same analyzer settings but do NOT set `GCC_TREAT_WARNINGS_AS_ERRORS` (keep warnings visible but non-blocking in debug).

3. Verify the project builds with zero warnings:
   ```bash
   cd AltTab && xcodebuild -project AltTab.xcodeproj -scheme AltTab -configuration Release build 2>&1 | grep -E 'warning:|error:|SUCCEEDED|FAILED'
   ```

4. Fix any warnings that surface from the static analyzer (if any).

Per D-24/D-25 (QA-07): Vulnerability scanning is deferred to Phase 5 per CONTEXT.md deferred section. SwiftLint's `force_cast`/`force_try`/`force_unwrapping` rules (configured in Plan 01) cover code-level security concerns for this phase. No Dependabot/CodeQL setup needed — zero external dependencies.
  </action>
  <verify>
    <automated>cd /Users/jmorales/workspace/apps/app.alttab-macos/AltTab && xcodebuild -project AltTab.xcodeproj -scheme AltTab -configuration Release build 2>&1 | grep -cE 'warning:' | xargs -I{} test {} -eq 0 && echo "ZERO_WARNINGS"</automated>
  </verify>
  <acceptance_criteria>
    - project.pbxproj contains `GCC_TREAT_WARNINGS_AS_ERRORS = YES` in Release configuration
    - project.pbxproj contains `RUN_CLANG_STATIC_ANALYZER = YES`
    - project.pbxproj contains `CLANG_ANALYZER_DEADCODE_DEADSTORES = YES`
    - Release build produces zero warnings
    - `make test` still passes
  </acceptance_criteria>
  <done>Static analysis enabled, warnings-as-errors in Release, zero build warnings</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all QA requirements end-to-end</name>
  <files>none</files>
  <action>
Human verification of all QA phase deliverables. No code changes — verification only.
  </action>
  <verify>
    <automated>cd /Users/jmorales/workspace/apps/app.alttab-macos && make lint 2>&1 | tail -1 && make test 2>&1 | grep -E 'SUCCEEDED|FAILED'</automated>
  </verify>
  <what-built>
Complete QA phase: SwiftLint with zero violations, window filtering fixes with debug logging, permission polling eliminated, hotkey thread safety added, static analysis enabled, force unwraps cleaned up.
  </what-built>
  <how-to-verify>
1. Run `make lint` — should exit cleanly with zero violations
2. Run `make test` — all tests should pass
3. Run `make build` — Release build should succeed with zero warnings
4. Run `make run-debug` — launch the app:
   - Open Settings window — verify no CPU spike (permission polling removed)
   - Check that "Refresh Status" button exists in Settings
   - Use Option+Tab — verify window list shows expected windows
   - Check /tmp/alttab-debug.log for "WindowEnum: Filtered" debug lines (confirms logging works)
5. Run `grep -r 'force_unwrap\|as! ' AltTab/AltTab/` — should only show documented SPI bridge exception
  </how-to-verify>
  <acceptance_criteria>
    - `make lint` exits 0
    - `make test` shows SUCCEEDED
    - `make build` shows SUCCEEDED with zero warnings
    - Settings window has "Refresh Status" button visible
    - No CPU spike when Settings window is open
  </acceptance_criteria>
  <done>User confirms all QA requirements are met via manual verification</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build system → source code | Static analyzer inspects all source at compile time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | E (Elevation of Privilege) | Static analysis | mitigate | Enable Clang static analyzer to catch memory/null issues at compile time |
| T-03-07 | T (Tampering) | Build settings | accept | project.pbxproj is version-controlled; changes reviewed in diff |
</threat_model>

<verification>
- `make lint` exits 0
- `make test` exits with SUCCEEDED
- Release build has zero warnings
- No SCShareableContent polling in SettingsWindow.swift
</verification>

<success_criteria>
- QA-01: SwiftLint config exists and runs clean
- QA-02: Zero SwiftLint violations
- QA-03: Static analysis enabled in Xcode
- QA-04: Window filtering debug logging added, self-filter uses PID
- QA-05: Permission polling timer removed, refresh button added
- QA-06: Force unwraps replaced or documented
- QA-07: SwiftLint security rules cover code-level concerns; Dependabot deferred to Phase 5 per D-24
- QA-08: All P1/P2 bugs from CONCERNS.md resolved (polling, filtering, hotkey thread safety)
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-assurance/03-03-SUMMARY.md`
</output>
